- Each file in `src/domain`, `src/services`, `src/connections`, `src/ui/components`, `src/ui/pages` must have a **1–3 sentence doc comment** at the top describing its role in the [[Domain Model]]
- Comments state **why**, not what — the code speaks for itself
- [technique] Test-driven design: write test first, then implement — see [[Tooling]]
- [decision] German-facing UI text; code and comments in English — see [[Domain Model]]

---

relates_to [[Architecture Overview]]
relates_to [[Tooling]]
relates_to [[Domain Model]]
